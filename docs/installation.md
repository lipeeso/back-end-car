# Instalação

1. Clone o repositório:
   ```bash
   git clone <URL>
   cd back_end_car
   ```
2. Instale as dependências:
   ```bash
   poetry install
   ```
3. Execute o projeto:
   ```bash
   poetry run task run
   ```
