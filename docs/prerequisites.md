# Pré-requisitos

Para rodar este projeto, você precisará de:

- **Python 3.13+**: O projeto utiliza recursos desta versão.
- **Poetry**: Gerenciador de dependências.
- **SQLite**: Banco de dados padrão utilizado (via `aiosqlite`).
