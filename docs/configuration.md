# Configuração do Projeto

As configurações do projeto são gerenciadas principalmente via variáveis de ambiente. Você pode criar um arquivo `.env` baseado em um exemplo (se houver) ou configurar as variáveis diretamente no sistema.

As principais configurações incluem:
- Variáveis de conexão com banco de dados.
- Configurações de JWT (Secret Key, Algorithm).
